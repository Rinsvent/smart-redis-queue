version: '3.8'

services:
  redis:
    container_name: redis
    image: redis:6.2.6
    networks:
      - requester-network
    ports:
      - 6379:6379
    restart: unless-stopped

networks:
  requester-network:
    driver: bridge

